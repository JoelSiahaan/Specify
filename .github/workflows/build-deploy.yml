name: Build and Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  NODE_VERSION: '20.19.0'

jobs:
  # Build and Push Docker Images
  build:
    name: Build Docker Images
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    permissions:
      contents: read
      packages: write
    
    outputs:
      backend-image: ${{ steps.meta-backend.outputs.tags }}
      frontend-image: ${{ steps.meta-frontend.outputs.tags }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract backend metadata
        id: meta-backend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/backend
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Backend image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: ${{ steps.meta-backend.outputs.tags }}
          labels: ${{ steps.meta-backend.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NODE_VERSION=${{ env.NODE_VERSION }}-alpine

      - name: Extract frontend metadata
        id: meta-frontend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/frontend
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: ${{ steps.meta-frontend.outputs.tags }}
          labels: ${{ steps.meta-frontend.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NODE_VERSION=${{ env.NODE_VERSION }}-alpine
            NGINX_VERSION=1.27-alpine

  # Deploy to Production (Optional - requires server setup)
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    # Uncomment when production server is ready
    # environment:
    #   name: production
    #   url: https://yourdomain.com
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      # Deployment via SSH (uncomment when ready)
      # - name: Deploy to production server
      #   uses: appleboy/ssh-action@v1.0.3
      #   with:
      #     host: ${{ secrets.PROD_HOST }}
      #     username: ${{ secrets.PROD_USER }}
      #     key: ${{ secrets.PROD_SSH_KEY }}
      #     port: ${{ secrets.PROD_SSH_PORT || 22 }}
      #     script: |
      #       cd /opt/lms
      #       echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      #       docker-compose -f docker-compose.prod.yml pull
      #       docker-compose -f docker-compose.prod.yml up -d
      #       docker-compose -f docker-compose.prod.yml exec -T backend npx prisma migrate deploy
      #       docker logout ghcr.io

      # Health check (uncomment when ready)
      # - name: Wait for deployment
      #   run: sleep 30

      # - name: Health check
      #   run: |
      #     response=$(curl -s -o /dev/null -w "%{http_code}" https://yourdomain.com/health)
      #     if [ $response -eq 200 ]; then
      #       echo "✅ Deployment successful - Health check passed"
      #     else
      #       echo "❌ Deployment failed - Health check returned $response"
      #       exit 1
      #     fi

      - name: Deployment placeholder
        run: |
          echo "🚀 Docker images built and pushed to GitHub Container Registry"
          echo "Backend image: ${{ needs.build.outputs.backend-image }}"
          echo "Frontend image: ${{ needs.build.outputs.frontend-image }}"
          echo ""
          echo "📝 To enable automatic deployment:"
          echo "1. Set up production server (AWS EC2, DigitalOcean, etc.)"
          echo "2. Add GitHub secrets: PROD_HOST, PROD_USER, PROD_SSH_KEY"
          echo "3. Uncomment deployment steps in this workflow"
          echo "4. Configure docker-compose.prod.yml on server"
          echo ""
          echo "📖 See DEPLOYMENT.md for detailed instructions"

  # Notify on failure
  notify-failure:
    name: Notify on Failure
    runs-on: ubuntu-latest
    needs: [build, deploy]
    if: failure()
    
    steps:
      - name: Notify failure
        run: |
          echo "❌ Build or deployment failed"
          echo "Check the workflow logs for details"
          # Add notification service here (Slack, Discord, email, etc.)
